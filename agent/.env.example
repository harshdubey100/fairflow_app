# Hugging Face token — used to call Qwen2.5-72B-Instruct via HF Inference Providers
HF_TOKEN=hf_your_token_here

# URL of the FairFlow Express backend (for agent tool calls)
BACKEND_URL=http://localhost:3000

# Port this agent service listens on
AGENT_PORT=8000
